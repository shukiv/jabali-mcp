# Tool reference

<!-- Generated by cmd/gen-tools -docs from openapi/openapi.yaml + the curation
     files. DO NOT EDIT — run 'make docs' after changing the tool surface. -->

Every tool takes an optional `panel` argument (fleet mode; omit for the default
panel). Write tools also take `dry_run` (preview the request without sending it),
and destructive tools act only when re-called with `confirm: true`.

## Read tools (always registered)

### list_domains

List your domains

`GET /domains`

| Argument | Type | Required | Description |
|---|---|---|---|
| `page` | integer |  | page number (1-based) |
| `page_size` | integer |  | results per page |
| `q` | string |  | search filter |

### get_domain

get_domain

`GET /domains/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### list_dns_records

List records in a zone

`GET /domains/{id}/dns/records`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### list_mailboxes

list_mailboxes

`GET /domains/{id}/mailboxes`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### list_forwarders

list_forwarders

`GET /domains/{id}/forwarders`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### list_applications

List installed apps

`GET /applications`

| Argument | Type | Required | Description |
|---|---|---|---|
| `page` | integer |  | page number (1-based) |
| `page_size` | integer |  | results per page |
| `q` | string |  | search filter |

### list_databases

list_databases

`GET /databases`

| Argument | Type | Required | Description |
|---|---|---|---|
| `page` | integer |  | page number (1-based) |
| `page_size` | integer |  | results per page |
| `q` | string |  | search filter |

### list_backups

List your snapshots

`GET /me/backups`

| Argument | Type | Required | Description |
|---|---|---|---|
| `page` | integer |  | page number (1-based) |
| `page_size` | integer |  | results per page |
| `q` | string |  | search filter |

### list_api_tokens

List your API tokens

`GET /me/api-tokens`

| Argument | Type | Required | Description |
|---|---|---|---|
| `page` | integer |  | page number (1-based) |
| `page_size` | integer |  | results per page |
| `q` | string |  | search filter |

### list_mail_logs

List recent mail log entries for your domains (delivered/failed/queued)

`GET /mail/logs`

| Argument | Type | Required | Description |
|---|---|---|---|
| `limit` | integer |  | max entries |
| `offset` | integer |  | offset |
| `sender` | string |  | filter by sender address |
| `recipient` | string |  | filter by recipient address |

### tail_web_log

Tail a domain's nginx access or error log (last N lines, snapshot)

`GET /logs/tail`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |
| `log_type` | string | ✓ | one of: access, error |
| `lines` | integer |  | how many trailing lines (min 1; max 2000) |

### get_ssl_status

SSL certificate status for a domain

`GET /domains/{id}/ssl`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### get_domain_whois

WHOIS lookup for a domain (registrar, expiry, name servers)

`GET /domains/{id}/whois`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### get_domain_bandwidth

Bandwidth usage for a domain (bytes + requests, daily series)

`GET /domains/{id}/bandwidth`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |
| `from` | string |  | start date YYYY-MM-DD (default 30 days ago) |
| `to` | string |  | end date YYYY-MM-DD (default today) |

### whoami

The calling account — id, email, package, limits

`GET /me`

### get_disk_usage

Disk usage breakdown — files, mail, databases vs quota

`GET /me/disk-usage`

### list_activity

Your recent activity (audit events, newest first)

`GET /me/activity`

| Argument | Type | Required | Description |
|---|---|---|---|
| `page` | integer |  | page number (1-based) |
| `page_size` | integer |  | results per page |
| `q` | string |  | search filter |

### list_cron_jobs

List your cron jobs

`GET /cron`

### get_cron_job

Get a cron job

`GET /cron/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `cron_id` | string | ✓ | the cron's ULID |

### get_cron_log

Read a cron job's recent log output

`GET /cron/{id}/log`

| Argument | Type | Required | Description |
|---|---|---|---|
| `cron_id` | string | ✓ | the cron's ULID |
| `lines` | integer |  | trailing lines to return (default 50) (min 1; max 500) |

### list_app_catalog

App catalog — every kind the panel can one-click install

`GET /applications/registry`

### get_application

Get an installed app (status, version, URL)

`GET /applications/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `application_id` | string | ✓ | the application's ULID |

### get_database

Get a database (engine, size, grants)

`GET /databases/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `databas_id` | string | ✓ | the databas's ULID |

### list_database_users

List your database users

`GET /database-users`

| Argument | Type | Required | Description |
|---|---|---|---|
| `page` | integer |  | page number (1-based) (min 1) |
| `page_size` | integer |  | results per page (default 20) (min 1; max 200) |

### get_mailbox

Get a mailbox (address, quota, flags)

`GET /mailboxes/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `mailbox_id` | string | ✓ | the mailbox's ULID |

### get_autoresponder

Get a mailbox's autoresponder (vacation reply)

`GET /mailboxes/{id}/autoresponder`

| Argument | Type | Required | Description |
|---|---|---|---|
| `mailbox_id` | string | ✓ | the mailbox's ULID |

### get_backup_manifest

List a snapshot's contents (manifest; only for succeeded backups)

`GET /me/backups/{id}/manifest`

| Argument | Type | Required | Description |
|---|---|---|---|
| `backup_id` | string | ✓ | the backup's ULID |

### list_ssh_keys

List your SSH keys (name + fingerprint; never the key material)

`GET /ssh-keys`

### get_php_settings

Effective PHP settings for a domain (version, limits)

`GET /domains/{id}/php-settings`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### list_php_versions

PHP runtimes installed on the server

`GET /php/versions`

### list_files

List a directory in your home (name, size, mtime per entry)

`GET /files`

| Argument | Type | Required | Description |
|---|---|---|---|
| `path` | string | ✓ | directory path inside your home, e.g. /public_html |

### preview_file

Read a file's contents (max 1 MiB; may expose secrets such as wp-config.php — handle with care)

`GET /files/preview`

| Argument | Type | Required | Description |
|---|---|---|---|
| `path` | string | ✓ | file path inside your home |

## Write tools (require `JABALI_MCP_ALLOW_WRITE=1`)

### create_domain

Add a domain

`POST /domains`

| Argument | Type | Required | Description |
|---|---|---|---|
| `name` | string | ✓ | e.g. example.com |

### update_domain

Update a domain's settings (partial — absent fields are left untouched)

`PATCH /domains/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |
| `is_enabled` | boolean |  | Enable or disable the site (vhost stays, serving stops). |
| `redirect_all_to` | string |  | Redirect the whole site to this URL (clearing a redirect is done in the panel UI). |
| `redirect_all_type` | string |  | HTTP status for the site-wide redirect. (one of: 301, 302, 307, 308) |
| `ssl_mode` | string |  | TLS mode: le = Let's Encrypt, self = self-signed, none = HTTP only. (custom/shared are managed via the panel's SSL settings.) (one of: le, self, none) |
| `temp_url_enabled` | boolean |  | Toggle the preview (temp) URL for this domain. |
| `webmail_enabled` | boolean |  | Toggle the webmail subdomain for this domain. |

### create_dns_record

Create a record

`POST /domains/{id}/dns/records`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |
| `content` | string | ✓ | e.g. 203.0.113.5 |
| `name` | string | ✓ | e.g. vpn |
| `priority` | integer |  | priority |
| `ttl` | integer |  | Omit to use server-wide default (Server Settings → DNS). |
| `type` | string | ✓ | one of: A, AAAA, CNAME, MX, TXT, SRV, CAA, NS |

### update_dns_record

Update a record

`PATCH /dns/records/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `record_id` | string | ✓ | the record's ULID |
| `content` | string |  | e.g. 203.0.113.99 |
| `is_enabled` | boolean |  | is enabled |
| `priority` | integer |  | priority |
| `ttl` | integer |  | ttl |

### create_mailbox

create_mailbox

`POST /domains/{id}/mailboxes`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |
| `local_part` | string | ✓ | e.g. alice |
| `password` | string | ✓ | password (min length 12) |
| `quota_mb` | integer | ✓ | e.g. 1024 |

### create_forwarder

create_forwarder

`POST /domains/{id}/forwarders`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |
| `dest` | string | ✓ | e.g. alice@example.com |
| `source` | string | ✓ | e.g. sales@example.com |

### create_backup

Trigger an on-demand backup

`POST /me/backups`

### install_application

Install an app (WordPress, Joomla, etc.)

`POST /applications`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | domain id |
| `kind` | string | ✓ | one of: wordpress, joomla, drupal, moodle, ghost, opencart, prestashop, magento, drupal10, mediawiki, nextcloud, mautic, suitecrm, dolibarr, processmaker |
| `path` | string |  | Install path under doc-root. |

### create_database

Create a database

`POST /databases`

| Argument | Type | Required | Description |
|---|---|---|---|
| `engine` | string | ✓ | one of: mysql, postgres |
| `name` | string | ✓ | e.g. myapp_prod |

### enable_ssl

Enable SSL — issue a Let's Encrypt certificate for the domain

`POST /domains/{id}/ssl`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### create_cron_job

Create a cron job

`POST /cron`

| Argument | Type | Required | Description |
|---|---|---|---|
| `command` | string | ✓ | shell command to run (as your user) |
| `enabled` | boolean |  | enabled |
| `name` | string | ✓ | job label |
| `schedule` | string | ✓ | cron schedule expression, e.g. '*/15 * * * *' |

### update_cron_job

Update a cron job (partial — absent fields are left untouched)

`PATCH /cron/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `cron_id` | string | ✓ | the cron's ULID |
| `command` | string |  | shell command to run (as your user) |
| `enabled` | boolean |  | enable or disable the job |
| `name` | string |  | job label |
| `schedule` | string |  | cron schedule expression, e.g. '*/15 * * * *' |

### run_cron_job

Run a cron job immediately (409 if the job is disabled)

`POST /cron/{id}/run-now`

| Argument | Type | Required | Description |
|---|---|---|---|
| `cron_id` | string | ✓ | the cron's ULID |

### create_database_user

Create a database user (returns its password once)

`POST /database-users`

| Argument | Type | Required | Description |
|---|---|---|---|
| `engine` | string |  | database engine (default mariadb) |
| `username` | string | ✓ | login name (your account prefix is enforced) |

### grant_database_access

Grant a database user access to one of your databases

`POST /database-users/{id}/grants`

| Argument | Type | Required | Description |
|---|---|---|---|
| `database_user_id` | string | ✓ | the database_user's ULID |
| `database_id` | string | ✓ | the database's ULID |
| `grant_level` | string | ✓ | rw = read-write, ro = read-only (one of: rw, ro) |

### update_mailbox

Update a mailbox (partial — absent fields are left untouched)

`PATCH /mailboxes/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `mailbox_id` | string | ✓ | the mailbox's ULID |
| `display_name` | string |  | display name shown in webmail |
| `is_disabled` | boolean |  | disable login + delivery for this mailbox |
| `quota_bytes` | integer |  | mailbox quota in bytes (min 16 MiB) (min 16777216) |
| `send_only` | boolean |  | mailbox may send but not receive |

### set_autoresponder

Set a mailbox's autoresponder (vacation reply)

`PUT /mailboxes/{id}/autoresponder`

| Argument | Type | Required | Description |
|---|---|---|---|
| `mailbox_id` | string | ✓ | the mailbox's ULID |
| `enabled` | boolean | ✓ | turn the autoresponder on or off |
| `from_date` | string |  | start (RFC 3339, e.g. 2026-08-10T00:00:00Z); omit for immediate |
| `html_body` | string |  | HTML reply body |
| `subject` | string |  | reply subject |
| `text_body` | string |  | plain-text reply body |
| `to_date` | string |  | end (RFC 3339); omit for open-ended |

### add_ssh_key

Add an SSH public key for SFTP/SSH access

`POST /ssh-keys`

| Argument | Type | Required | Description |
|---|---|---|---|
| `name` | string | ✓ | key label, e.g. laptop |
| `public_key` | string | ✓ | OpenSSH public key line (ed25519 or RSA >= 2048 bits) |

### update_php_settings

Update a domain's PHP settings (partial — absent fields are left untouched)

`PATCH /domains/{id}/php-settings`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |
| `php_max_execution_time` | integer |  | script time limit, seconds (min 1; max 86400) |
| `php_max_input_time` | integer |  | input parsing time limit, seconds (min 1; max 86400) |
| `php_max_input_vars` | integer |  | max request input variables (min 1; max 86400) |
| `php_memory_limit` | string |  | digits + optional K/M/G suffix, e.g. 256M |
| `php_post_max_size` | string |  | digits + optional K/M/G suffix, e.g. 64M |
| `php_upload_max_filesize` | string |  | digits + optional K/M/G suffix, e.g. 64M |
| `php_version` | string |  | e.g. 8.3 — must be installed on the server (see list_php_versions); changes the pool shared by ALL your domains |

## Destructive tools (write + `confirm: true`)

### delete_domain

delete_domain

`DELETE /domains/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### delete_dns_record

Delete a record

`DELETE /dns/records/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `record_id` | string | ✓ | the record's ULID |

### delete_mailbox

delete_mailbox

`DELETE /mailboxes/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `mailbox_id` | string | ✓ | the mailbox's ULID |

### set_mailbox_password

Change mailbox password

`PUT /mailboxes/{id}/password`

| Argument | Type | Required | Description |
|---|---|---|---|
| `mailbox_id` | string | ✓ | the mailbox's ULID |
| `password` | string | ✓ | password (min length 12) |

### restore_backup

Restore a snapshot

`POST /me/backups/{id}/restore`

| Argument | Type | Required | Description |
|---|---|---|---|
| `backup_id` | string | ✓ | the backup's ULID |

### disable_ssl

Disable SSL — revoke the domain's certificate and stop serving HTTPS

`DELETE /domains/{id}/ssl`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### delete_cron_job

Delete a cron job

`DELETE /cron/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `cron_id` | string | ✓ | the cron's ULID |

### delete_application

Uninstall an app and remove its files (async)

`DELETE /applications/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `application_id` | string | ✓ | the application's ULID |

### delete_database

Drop a database and all its data

`DELETE /databases/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `databas_id` | string | ✓ | the databas's ULID |

### delete_database_user

Delete a database user (revokes all its grants)

`DELETE /database-users/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `database_user_id` | string | ✓ | the database_user's ULID |

### revoke_database_access

Revoke one database grant (keeps the user)

`DELETE /database-user-grants/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `database_user_grant_id` | string | ✓ | the database_user_grant's ULID |

### rotate_database_password

Rotate a database user's password (returns the new password once)

`POST /database-users/{id}/rotate-password`

| Argument | Type | Required | Description |
|---|---|---|---|
| `database_user_id` | string | ✓ | the database_user's ULID |
| `new_password` | string | ✓ | the new password to set |

### delete_autoresponder

Remove a mailbox's autoresponder

`DELETE /mailboxes/{id}/autoresponder`

| Argument | Type | Required | Description |
|---|---|---|---|
| `mailbox_id` | string | ✓ | the mailbox's ULID |

### rotate_mailbox_password

Rotate a mailbox password (returns the new password once)

`POST /mailboxes/{id}/rotate-password`

| Argument | Type | Required | Description |
|---|---|---|---|
| `mailbox_id` | string | ✓ | the mailbox's ULID |
| `new_password` | string |  | omit to auto-generate a strong password |

### delete_backup

Delete a snapshot (forgets + prunes its data)

`DELETE /me/backups/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `backup_id` | string | ✓ | the backup's ULID |

### delete_ssh_key

Remove an SSH key (revokes its access)

`DELETE /ssh-keys/{id}`

| Argument | Type | Required | Description |
|---|---|---|---|
| `ssh_key_id` | string | ✓ | the ssh_key's ULID |

## Hand-written tools

Always registered alongside the generated read tools.

### diagnose_domain

One-call domain triage: config, SSL certificate status, DNS records, recent
nginx error-log lines, and bandwidth in a single JSON answer. A failed probe is
reported under `probe_errors` without failing the rest; an SSL 404 maps to
`{"status": "none"}` (no certificate is a diagnosis, not an error).

| Argument | Type | Required | Description |
|---|---|---|---|
| `panel` | string | | target panel name for fleet mode; omit to use the default panel |
| `domain_id` | string | ✓ | the domain's ULID |
| `error_lines` | integer | | trailing nginx error-log lines to include (default 50, max 500) |

### report_issue

Draft or file a GitHub issue (bug report or feature request) on
[jabali-panel](https://github.com/shukiv/jabali-panel/issues) or
[jabali-mcp](https://github.com/shukiv/jabali-mcp/issues). In read-only mode it
only returns a prefilled `issues/new` link for the user to review and submit.
In write mode, direct creation via the `gh` CLI additionally requires
`confirm: true` (it posts under the user's GitHub identity); `dry_run` previews.
Text containing panel/GitHub token patterns is refused. Set
`diagnose_domain_id` to auto-attach the `diagnose_domain` probe results as
evidence.

| Argument | Type | Required | Description |
|---|---|---|---|
| `panel` | string | | target panel name for fleet mode; omit to use the default panel |
| `repo` | string | ✓ | one of: jabali-panel (panel/API behavior), jabali-mcp (MCP tool problems) |
| `kind` | string | ✓ | one of: bug, feature |
| `title` | string | ✓ | one-line issue title |
| `body` | string | ✓ | markdown body — never include tokens, passwords, or file contents |
| `diagnose_domain_id` | string | | domain ULID: attach diagnose_domain results (config, SSL, DNS, recent errors) |
| `confirm` | boolean | | create directly via gh (write mode only) |
| `dry_run` | boolean | | preview only |

### search_docs

Search the official Jabali Panel documentation and return the relevant
sections, so the assistant can consult docs mid-task and then act with the
panel tools. Reads `<docs-base>/llms.txt` (default base
`https://jabali-panel.com`, override with `JABALI_DOCS_URL`) and fetches
matching markdown pages. Fetches are anonymous — the panel token is never
sent — and restricted to the docs origin; off-origin links in the index are
ignored.

| Argument | Type | Required | Description |
|---|---|---|---|
| `query` | string | ✓ | what to look up, e.g. "configure MTA-STS" |
| `doc` | string | | doc path/URL from a previous result to fetch in full |

## Admin tools (require `JABALI_MCP_ADMIN=1` + an admin token)

### admin_list_users

List all panel users (admin)

`GET /admin/users`

| Argument | Type | Required | Description |
|---|---|---|---|
| `page` | integer |  | page number (1-based) |
| `page_size` | integer |  | results per page |
| `q` | string |  | search filter |

### admin_create_user

Create a tenant user (admin)

`POST /admin/users`

| Argument | Type | Required | Description |
|---|---|---|---|
| `email` | string | ✓ | email |
| `is_admin` | boolean |  | is admin |
| `password` | string | ✓ | password (min length 12) |

### admin_get_settings

Read server-wide settings (admin)

`GET /admin/settings`

### admin_update_settings

Update server-wide settings (admin)

`PATCH /admin/settings`

| Argument | Type | Required | Description |
|---|---|---|---|
| `default_dns_ttl` | integer |  | Server-wide default TTL for new DNS records (seconds). (min 60; max 86400) |
| `hostname` | string |  | hostname |

### admin_run_updates

Trigger `jabali update` (admin)

`POST /admin/updates/run`

### admin_renew_ssl

Force-renew a domain's certificate (admin)

`POST /domains/{id}/ssl/renew`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

### admin_retry_ssl

Retry a failed certificate issuance (admin)

`POST /domains/{id}/ssl/retry`

| Argument | Type | Required | Description |
|---|---|---|---|
| `domain_id` | string | ✓ | the domain's ULID |

